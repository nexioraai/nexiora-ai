import { Relation } from '@/erp/contracts/Relation'

export function buildRelations(
modelName: string,
relations: Relation[]
): string {

// Phase 1 :
// On conserve les relations dans le Blueprint,
// mais on ne génère plus de relations Prisma.

return ''
}
