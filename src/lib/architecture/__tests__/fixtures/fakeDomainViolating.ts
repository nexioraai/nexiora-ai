// Fixture jetable pour le Bloc 5 (preuve d'extensibilité de
// checkDomainBoundaries). Contient délibérément, une seule fois plus bas,
// le motif interdit défini par extensibilityProof.test.ts, pour prouver
// que le moteur détecte une violation sur un domaine totalement inventé,
// sans rapport avec Mode 1 ou le panier.
export const value = 'FORBIDDEN_TOKEN_FOR_TEST apparaît ici'
