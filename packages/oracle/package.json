{
  "name": "@deribfy/oracle",
  "version": "0.1.0",
  "private": true,
  "description": "Oracle v1 (Phase 6, D-034) — service SEPARE qui LIT les artefacts et re-verifie de maniere DETERMINISTE (§9 niveau 1), sans jamais faire confiance au generateur. Aucune autorite LLM sur ce niveau.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings 0",
    "test": "vitest run"
  },
  "dependencies": {
    "@deribfy/air-schema": "*",
    "@deribfy/capability-registry": "*",
    "@deribfy/compiler": "*",
    "@deribfy/execution-contract": "*",
    "@deribfy/provisioner": "*",
    "@deribfy/slots": "*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^20",
    "eslint": "^9.39.0",
    "typescript": "^5",
    "typescript-eslint": "^8.60.0",
    "vitest": "^4.1.10"
  }
}
