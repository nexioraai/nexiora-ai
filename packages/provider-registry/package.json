{
  "name": "@deribfy/provider-registry",
  "version": "0.1.0",
  "private": true,
  "description": "Abstraction provider v1 (Phase 10, ARCHITECTURE §15) — classes de provider CANONIQUES derivees du registre de capabilities gele, selection deterministe et substitution prouvee sans changement d'AIR.",
  "type": "module",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings 0",
    "test": "vitest run"
  },
  "dependencies": {
    "@deribfy/capability-registry": "*"
  },
  "devDependencies": {
    "@types/node": "^20",
    "eslint": "^9.39.0",
    "typescript": "^5",
    "typescript-eslint": "^8.60.0",
    "vitest": "^4.1.10"
  }
}
