{
  "name": "@deribfy/capability-registry",
  "version": "0.1.0",
  "private": true,
  "description": "Capability Registry v1 — registre fermé des capacités du moteur : le LLM demande, le registre décide (classe commerce, empreinte native, permissions induites, OTA/rebuild)",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings 0",
    "test": "vitest run"
  },
  "dependencies": {
    "@deribfy/air-schema": "*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^20",
    "eslint": "^9.39.0",
    "typescript": "^5",
    "typescript-eslint": "^8.60.0",
    "vitest": "^4.1.10"
  }
}
