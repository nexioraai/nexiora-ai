import { createFixtureApp } from "../fixture-core/app-shell";
import { Root, primitives, useThemeBridge } from "./candidate";

export default createFixtureApp({ name: "tamagui", primitives, useThemeBridge, Root });
